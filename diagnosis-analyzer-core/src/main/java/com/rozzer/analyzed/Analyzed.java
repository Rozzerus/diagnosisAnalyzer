package com.rozzer.analyzed;

import com.rozzer.common.Identity;
import com.rozzer.common.Saved;

/**
 * If Objects have the interface them can be analyzed and saved in DB
 */
public interface Analyzed extends Identity, Saved {
}
